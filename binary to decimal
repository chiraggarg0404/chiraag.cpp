#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int n,y,p=0,s=0;
cout << "enter binary number";
cin >> n;
while(n!=0)
{y=n%10;
s=s+pow(2,p)*y;
p+=1;
n=n/10;
}
cout << s;
    return 0;
}
