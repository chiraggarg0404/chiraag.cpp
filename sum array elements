#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int a[3][3],i,j,sum=0;
    for(i=0;i<3;i++)
    {
        for(j=0;j<3;j++)
        {
        cin>>a[i][j];
        sum=sum+a[i][j];}
    }
   cout << sum;

    return 0;
}
